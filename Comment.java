class Comment {
    // This is a end-of-line comment; it extends to the end of the line.

    /* This is a delimited comment,
    extending over several line
    */
    int /* This delimited comment extends over part of a line */ x = 117;
}